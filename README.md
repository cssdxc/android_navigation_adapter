# android_navigation_adapter

Flutter plugin for consistent bottom action spacing and Android navigation mode detection.

The package channel is `android_navigation_adapter`. It contains no host-app code or feature-specific names.
